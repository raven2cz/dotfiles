inputs generated by performing an empirical distribution estimate for BSE (barsSinceEntry). This distribution randomly generates BSEs, which are added to the inputs of the strategy and thus define the output.