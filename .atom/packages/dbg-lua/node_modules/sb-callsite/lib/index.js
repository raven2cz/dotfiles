"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.fromStack = fromStack;
exports.capture = capture;
const extractionRegex = /at (\S+) \(([\S ]+):(\d+):(\d+)\)/;

function fromStack(stack) {
  const stackChunks = stack.split('\n');
  const traces = [];
  stackChunks.forEach(function (entry) {
    const matches = extractionRegex.exec(entry);

    if (matches !== null) {
      traces.push({
        function: matches[1],
        file: matches[2],
        line: parseInt(matches[3], 10) || 1,
        col: parseInt(matches[4], 10) || 1
      });
    }
  });
  return traces;
}

function capture() {
  const traces = fromStack(new Error().stack); // Remove self from the stack

  traces.shift();
  return traces;
}